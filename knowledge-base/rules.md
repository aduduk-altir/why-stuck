# Knowledge Base — Stub

Drop knowledge documents in this folder. They're loaded into the system prompt at request time
by `src/app/api/chat/route.ts`.

Suggested files for the hackathon:

- `form-rules.md` — business rules / validation expectations
- `routing-map.md` — what URLs / screens mean what
- `troubleshooting.md` — fixes for known stuck states

For now this single file acts as the placeholder so `fs.readFileSync` doesn't blow up.
